#include<iostream>
using namespace std;
int main(){
    char str[100];
    int length; 
    cout<<"Enter The String = ";
    cin.getline(str,100);
    
    for(length = 0; str[length]!= '\0'; ++length){
        
    }
    cout<<"LENGTH = "<<length<<endl;
    
}
